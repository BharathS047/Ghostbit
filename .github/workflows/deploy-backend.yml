name: Deploy Backend to Azure App Service

on:
  push:
    branches: [main]
    paths:
      - 'ghostbit/backend/**'
      - 'ghostbit/core/**'
      - 'ghostbit/stego/**'
      - 'ghostbit/metrics/**'
      - 'ghostbit/app/**'
      - 'ghostbit/__init__.py'
      - 'requirements.txt'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create deployment package
        run: |
          zip -r deploy.zip \
            ghostbit/__init__.py \
            ghostbit/app/ \
            ghostbit/backend/ \
            ghostbit/core/ \
            ghostbit/metrics/ \
            ghostbit/stego/ \
            requirements.txt \
            -x "**/__pycache__/*" "**/*.pyc" "**/*.db"

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: ghostbit-api
          package: deploy.zip
