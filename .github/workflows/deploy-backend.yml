name: Deploy Backend to Azure App Service

on:
  push:
    branches: [main]
    paths:
      - 'ghostbit/backend/**'
      - 'ghostbit/core/**'
      - 'ghostbit/stego/**'
      - 'ghostbit/metrics/**'
      - 'ghostbit/app/**'
      - 'ghostbit/__init__.py'
      - 'requirements.txt'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r requirements.txt

      - name: Create deployment package
        run: |
          zip -r deploy.zip \
            ghostbit/__init__.py \
            ghostbit/app/ \
            ghostbit/backend/ \
            ghostbit/core/ \
            ghostbit/metrics/ \
            ghostbit/stego/ \
            requirements.txt \
            antenv/ \
            -x "**/__pycache__/*" "**/*.pyc" "**/*.db"

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to App Service
        run: |
          az webapp deploy --resource-group ghostbit-rg --name ghostbit-api \
            --src-path deploy.zip --type zip --async true
